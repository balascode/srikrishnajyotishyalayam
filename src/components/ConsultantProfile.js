import React from 'react';
import '../assets/css/main.css';

const ConsultantProfile = ({ match }) => {
  // Use match.params.id to fetch and display consultant details
  return (
    <div className="content">
      <section className="section">
        <h1>Consultant Profile</h1>
        {/* Display detailed information about the consultant */}
      </section>
    </div>
  );
};

export default ConsultantProfile;
